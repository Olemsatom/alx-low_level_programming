#include "main.h"

/**
* print_sign - print the sign of a number
* @n: sign
* Return: 0
*/


int print_sign(int n)

{

if (n > 0)
_pitchar("+");
return (1);

else (n < 0)
_putchar("-");
return (-1);

else (n == 0)
_putchar("0")
return (0);

}
