#include "main.h"

/**
* print_sign - print the sign of a number
* @n: integer
* Return: 0
*/


int print_sign(int n)

{
char n

if (n > 0)
_pitchar("+");
return (1);

if (n < 0)
_putchar("-");
return (-1);

if (n == 0)
_putchar("0")
return (0);

}
